{
  "module": "v_homeDynamic",
  "layout": {
    "dynamic": {
      "initRequest": {
        "url": "/views/cs/collManage/tcManage/collWorkbench/collWorkbench_main_info.json",
        "method": "POST",
        "params": {
          "case_id": "${{params.case_id}}",
          "case_no": "${{params.case_no}}",
          "cust_id": "${{params.cust_id}}",
          "org_id": "${{params.org_id}}",
          "oa_org_id": "${{params.oa_org_id}}",
          "cert_no": "${{params.cert_no}}",
          "cert_type": "${{params.cert_type}}",
          "queue_code": "${{params.queue_code}}",
          "product_type": "${{params.product_type}}"
        },
        "resultSet": {
          "spanField": "span",
          "heightField": "height",
          "borderField": "border",
          "urlField": "pageCode"
        }
      }
    }
  }
}

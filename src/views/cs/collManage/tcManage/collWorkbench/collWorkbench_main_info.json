{
  "code": "200",
  "message": "操作成功",
  "data": [
    {
      "pageCode": "/views/cs/collManage/tcManage/collWorkbench/mainMenu/basic_info/basic_info_management.json",
      "span": 24,
      "height": "430",
      "border": "0",
      "params": {
        "cust_id": "${{params.cust_id}}",
        "case_no": "${{params.case_no}}",
        "org_id": "${{params.org_id}}",
        "oa_org_id": "${{params.oa_org_id}}",
        "cert_no": "${{params.cert_no}}",
        "cert_type": "${{params.cert_type}}",
        "case_id": "${{params.case_id}}",
        "ishide1": "'${{params.product_type}}' == '1'",
        "ishide2": "'${{params.product_type}}' == '2'"
      }
    },
    {
      "pageCode": "@cardlist",
      "span": 24,
      "height": "1200",
      "border": "0",
      "params": {
        "case_id": "${{params.case_id}}",
        "case_no": "${{params.case_no}}",
        "cust_id": "${{params.cust_id}}",
        "org_id": "${{params.org_id}}",
        "oa_org_id": "${{params.oa_org_id}}",
        "cert_no": "${{params.cert_no}}",
        "cert_type": "${{params.cert_type}}",
        "queue_code": "${{params.queue_code}}",
        "ishide1": "'${{params.product_type}}' == '1'",
        "ishide2": "'${{params.product_type}}' == '2'"
      }
    }
  ]
}
